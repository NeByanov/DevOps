name: ansible-validate
on:
  push:
    branches: [ main ]
    paths:
      - 'ansible/**'
      - '.github/workflows/ansible-validate.yaml'
  pull_request:
    branches: [ main ]
    paths:
      - 'ansible/**'
      - '.github/workflows/ansible-validate.yaml'

  workflow_dispatch:

defaults:
  run:
    working-directory: ./ansible

jobs:
  syntax-check:
    name: syntax-check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up SSH
        run: |
          echo "${{ secrets.SSHPRIVATEKEY }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Install Ansible
        shell: bash
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Syntax-Check
        run: |
          ansible-playbook --syntax-check site.yml
          ansible-playbook --syntax-check verify.yml
          ansible-playbook --syntax-check site-backup.yml
          ansible-playbook --syntax-check verify-logrotate.yaml

      - name: Clean up SSH key
        if: always()
        run: rm -f private_key.pem

  deploy:
    name: Deploy
    runs-on: self-hosted
    timeout-minutes: 15
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: syntax-check

    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up SSH
        run: |
          echo "${{ secrets.SSHPRIVATEKEY }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Install Ansible
        shell: bash
        run: |
          sudo apt update
          sudo apt install -y ansible

      - name: Run Ansible playbook
        run: |
          ansible-playbook verify.yml -i inventory.yml
          ansible-playbook verify-logrotate.yaml -i inventory.yml


      - name: Clean up SSH key
        if: always()
        run: rm -f private_key.pem


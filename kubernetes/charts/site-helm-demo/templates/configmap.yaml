apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "site-helm-demo.fullname" . }}-content
  labels:
    {{- include "site-helm-demo.labels" . | nindent 4 }}
data:
  index.html: |
{{ .Values.pageText | nindent 4 }}

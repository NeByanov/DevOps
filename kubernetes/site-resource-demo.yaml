apiVersion: v1
kind: ResourceQuota
metadata:
  name: site-resource-demo
  namespace: site-lab
spec:
  hard:
    pods: "4"
    requests.memory: "1G"
    requests.cpu: "2"
    limits.memory: "4G"
    limits.cpu: "3.5"


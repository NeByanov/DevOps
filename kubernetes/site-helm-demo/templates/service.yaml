apiVersion: v1
kind: Service
metadata:
  name: {{ include "site-helm-demo.fullname" . }}
  labels:
    {{- include "site-helm-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "site-helm-demo.selectorLabels" . | nindent 4 }}
